export class Car {
    public constructor(
        public id: number,
        public plateNumber: string,
        public model: string,
        public year: number,
        public color: string
    ) {}
}